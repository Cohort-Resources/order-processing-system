spring:
  application:
    name: customer-service
  datasource:
    password: ${DATABASE_PASSWORD:password}
    username: ${DATABASE_USER:default}
  profiles:
    active: dev
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: false
